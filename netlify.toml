[build]
  command = "vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — redirect all paths to index.html so React Router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
